server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://192.168.220.200:3306/article_demo?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: cpc!23#@
    driver-class-name: com.mysql.cj.jdbc.Driver

  kafka:
    bootstrap-servers: 192.168.220.200:9092
    consumer:
      group-id: article-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

app:
  kafka:
    topic:
      article-publish: article.publish
