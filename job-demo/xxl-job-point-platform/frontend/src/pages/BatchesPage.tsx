import {useEffect,useState} from 'react';import {Card,Table,Typography} from 'antd';import {getBatches} from '../services/api';import {StatusTag} from '../components/StatusTag';
export default function BatchesPage(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{getBatches().then(setRows)},[]);return <Card><Typography.Title level={3}>XXL-JOB 发放批次</Typography.Title><Table rowKey="id" dataSource={rows} columns={[{title:'批次ID',dataIndex:'id'},{title:'业务类型',dataIndex:'biz_type'},{title:'奖励日期',dataIndex:'reward_date'},{title:'状态',dataIndex:'status',render:v=><StatusTag value={v}/>},{title:'分片',dataIndex:'shard_total'},{title:'扫描',dataIndex:'scanned_count'},{title:'Kafka发送',dataIndex:'sent_count'},{title:'开始时间',dataIndex:'started_at'},{title:'结束时间',dataIndex:'finished_at'},{title:'备注',dataIndex:'remark'}]}/></Card>}
