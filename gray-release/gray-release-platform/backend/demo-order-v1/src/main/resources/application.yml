server:
  port: 18081

spring:
  application:
    name: demo-order-service
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.1.200:8848
        metadata:
          version: v1
      config:
        server-addr: 192.168.1.200:8848
        import-check:
          enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
