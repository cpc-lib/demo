server:
  port: 18082

spring:
  application:
    name: demo-order-service
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.220.200:8848
        metadata:
          version: v2
      config:
        server-addr: 192.168.220.200:8848
        import-check:
          enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
