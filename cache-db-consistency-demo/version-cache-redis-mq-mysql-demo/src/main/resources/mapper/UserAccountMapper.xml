<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.versionedcachemp.mapper.UserAccountMapper">

    <resultMap id="BaseResultMap" type="com.example.versionedcachemp.domain.UserAccount">
        <id    column="id"          property="id"/>
        <result column="username"   property="username"/>
        <result column="balance"    property="balance"/>
        <result column="version"    property="version"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, username, balance, version, update_time
    </sql>

    <!-- 给用户加钱，版本号 +1 -->
    <update id="increaseBalance">
        UPDATE user_account
        SET balance = balance + #{amount}, version = version + 1
        WHERE id = #{id}
    </update>

</mapper>
